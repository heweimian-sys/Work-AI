# 开发计划

## 当前阶段

- [x] 初始化标准目录结构
- [x] 建立项目文档模板
- [x] 初始化 Git 工作流
- [x] 绑定工作仓库 GitHub remote
- [x] 修正仓库定位为工作 AI 开发总仓库
- [ ] 接入飞书 CLI 相关代码
- [ ] 接入航海小抓相关代码
- [ ] 明确工作 AI 项目清单与优先级
- [ ] 设计 Agent 与 MCP 的基础架构
- [ ] 建立首个工作 AI 模块的最小可运行原型
- [ ] 补充自动化测试与验证流程

## 下一步

1. 获取飞书 CLI 与航海小抓的源码位置或压缩包。
2. 按职责放入 `MCP/`、`Agent/`、`后端/` 或 `技术文档/`。
3. 明确工作 AI 项目清单、优先级、输入输出和权限边界。
4. 梳理各模块产品需求，补充到 `产品需求.md`。
5. 根据需求拆分前端、后端、MCP 与 Agent 的第一轮开发任务。

## 项目与模块接入计划

| 模块 | 建议目录 | 当前状态 | 说明 |
| --- | --- | --- | --- |
| 飞书 CLI | `MCP/飞书CLI/` 或 `后端/飞书CLI/` | 待接入 | 如果主要提供工具调用能力，优先放入 `MCP/`；如果主要是脚本或服务，放入 `后端/`。 |
| 航海小抓 | `Agent/航海小抓/` 或 `后端/航海小抓/` | 待接入 | 作为工作 AI 子模块接入，不代表整个仓库定位。若包含抓取策略和 Agent 编排，优先放入 `Agent/`；如果是独立抓取程序，放入 `后端/`。 |

## 维护规则

- 每次开发结束后更新本文件的任务状态。
- 新增需求必须先进入计划，再进入开发。
- 如果计划与实际实现不一致，优先更新计划再继续开发。
